# 🎮 Chat Plays Roblox (via Wine + FFmpeg)

> **Автоматизированная трансляция Roblox через Wine на Linux с захватом экрана DISPLAY=:0**  
> Часть медиаэкспериментов **D58 Records** • LostTip Entertainment

---

## 📌 Описание

Этот проект позволяет:
- Запускать **Roblox Player** в изолированном окружении **Wine** на Linux,
- Перехватывать вывод X11 (`DISPLAY=:0`) с помощью **FFmpeg**,
- Транслировать игровой процесс в реальном времени (например, на YouTube или Twitch).

Используется в рамках интерактивных стримов «Chat Plays» без прямого участия Windows.

> ⚠️ **Важно**:  
> Этот репозиторий **не содержит** и **не распространяет** `Roblox.exe`.  
> Пользователь обязан самостоятельно соблюдать [Условия использования Roblox](https://en.help.roblox.com/hc/en-us/articles/115004647846).


---

## ⚙️ Требования

- Linux (проверено на Ubuntu 22.04 / Debian 12)
- `wine` ≥ 8.0
- `winetricks`
- `ffmpeg` ≥ 5.0
- X-сервер (реальный или виртуальный, например `Xvfb`)
- Доступ к `DISPLAY=:0` (или настройка виртуального дисплея)

---

## 🚀 Быстрый старт

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/bludjetelksina/chat-plays-roblox.git
   cd chat-plays-roblox
   ```
2. **Создайте Wine-префикс**
   ```bash
   chmod +x scripts/setup_wine.sh
   ./scripts/setup_wine.sh
   ```
3. **Поместите RobloxPlayer.exe в assets/**
(Скачайте его через официальный лаунчер на Windows или из временной папки)
4. **Настройте трансляцию**
   ```bash
   cp config/ffmpeg_stream.conf.example config/ffmpeg_stream.conf
   nano config/ffmpeg_stream.conf  # Укажите ваш RTMP URL
   ```
5. **Запустите игру и стрим**
   ```bash
   # В первом терминале:
   ./scripts/launch_roblox.sh

   # Во втором:
   ./scripts/start_stream.sh
   ```

💡 Совет: Для headless-сервера используйте **Xvfb :0 -screen 0 1280x720x24 &** перед запуском.

